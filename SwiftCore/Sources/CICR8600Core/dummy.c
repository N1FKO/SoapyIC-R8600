// Intentionally empty. This target exists only to publish icr8600_core.h
// as a stable C header that both ICR8600Core (Swift, via @_cdecl) and the
// C++ SoapySDR module can share. No implementation lives here.
